# Public API base URL (no trailing slash). Set this in your host’s build env.
# Example after you deploy the backend:
# VITE_API_URL=https://devtinder-api.onrender.com

VITE_API_URL=http://localhost:7777
